<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RemoveBackground" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RemoveBackground" parent="Base.Theme.RemoveBackground" />

    <!-- Base application theme. -->
    <style name="Base.Theme.GeminiApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize Bottom Navigation View color -->
        <!--        HINH fix status header bar-->
        <!--        <item name="colorPrimary">@color/primaryColor</item>-->
        <!--        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>-->
        <!--        <item name="android:statusBarColor">@color/primaryColor</item>-->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_app</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.GeminiApp</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!-- Thêm các thuộc tính khác tùy ý -->
    </style>

</resources>